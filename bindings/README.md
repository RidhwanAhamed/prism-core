# bindings

Empty until Task 6: `dart/` arrives via ffigen against the public C ABI
(`include/prism/prism_core.h`, Task 5). Bindings capture and forward raw
events only — no inference logic lives here (CLAUDE.md: thin shells).
